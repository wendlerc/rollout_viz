[project]
name = "rollout-viz"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["streamlit", "pip", "ipykernel", "plotly", "numpy"]
package-mode = false
